export const myName = 'Trí Khải';

export const sayHello = (loopCount) => {
    for(let i = 0; i < loopCount; i++) {
        console.log('Hello, I am ' + myName);
    }
};

//export {myName, sayHello};